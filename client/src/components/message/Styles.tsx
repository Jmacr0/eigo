import styled from 'styled-components';
import { Snackbar } from '@material-ui/core';
import Alert from '@material-ui/lab/Alert'

export const MessageWrapper = styled(Snackbar)``;

export const MessageContent = styled(Alert)``;